#datasource specific properties
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/academymicrodb

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true 
  application:
    name: PRODUCT-CATALOG
